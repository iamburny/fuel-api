# Postgres — POSTGRES_PASSWORD is read by the postgres container,
# DATABASE_URL is read by the API container. Keep the password in sync.
POSTGRES_PASSWORD=change-this-to-a-strong-password
DATABASE_URL=postgresql://fuel:change-this-to-a-strong-password@fuel-db:5432/fueldb

# Gov Fuel Finder API credentials
FUEL_FINDER_CLIENT_ID=your-client-id
FUEL_FINDER_CLIENT_SECRET=your-client-secret

# Polling interval (minutes) — minimum 5 per Fair Use Policy
POLL_INTERVAL_MINUTES=30

# JWT auth for app users
JWT_SECRET=generate-a-long-random-string-here
